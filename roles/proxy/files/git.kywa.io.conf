server {

    listen 443;
    
    server_name git.kywa.io;
    
    ssl_certificate /etc/letsencrypt/live/kywa.io/cert.pem;
    ssl_certificate_key /etc/letsencrypt/live/kywa.io/privkey.pem;
    ssl on;
    ssl_session_cache builtin:1000 shared:SSL:10m;
    ssl_protocols TLSv1 TLSv1.1 TLSv1.2;
    ssl_ciphers HIGH:!aNULL:!eNULL:!EXPORT:!CAMELLIA:!DES:!MD5:!PSK:!RC4;
    ssl_prefer_server_ciphers on;
    
    location / {

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        proxy_pass https://git.kywa.io;
        proxy_read_timeout 90;

    }
    
}
#            proxy_pass http://localhost:8080;
#            proxy_redirect http://localhost:8080 https://git.kywa.io;
